pipeline {
    agent any
    stages {
        stage('Load Utilities') {
            steps {
                script {
                    // Load the config.groovy script and call getConfig function
                    def configScript = load "pipeline_utils.groovy"
                    def config = configScript.getConfig()

                    // Store the configuration in environment variables for later use
                    // Add other environment variables as needed
                }
            }
        }

        stage('Prep work') {
            parallel {
                stage('Clean up folder') {
                    steps {
                        script {
                   

                    echo "Slack Channel: config.SLACK_CHANNEL"
                        }
                    }
                }
            }
        }
    }
}
