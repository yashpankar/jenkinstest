pipeline {
    agent any

    stages {
        stage('Load and Hello') {
            steps {
                script {
                    // Load the Groovy script
                    def helloWorld = load 'helloWorld.groovy'
                    
                    // Call the function from the loaded script
                    helloWorld.helloWorld()
                }
            }
        }
    }
}

